const exe = '      Melhor exemplo do Mundo        '

// Quantidade de String
console.log(exe.length)

// Converte para Maiúsculo
console.log(exe.toUpperCase())

// Progurar um trecho
console.log(exe.indexOf('plo'))

// Recortar uma parte da string por posição
console.log(exe.slice(0, 6))

//remove espaços nas extremidades
console.log(exe.trim())