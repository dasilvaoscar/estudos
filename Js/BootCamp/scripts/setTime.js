function show() {
    console.log('Show bimarino!');
}

const interval = setInterval(show, 2) // Executa a função após o tempo determinado;

const timeout = setTimeout(show, 10001) // Executa o script UMA vez como um deley informado;